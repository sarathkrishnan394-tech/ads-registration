// Placeholder committed to the repo so the site never 404s on this file.
// On Vercel, `npm run build` (scripts/build-env.js) overwrites this with
// real values from your SUPABASE_URL / SUPABASE_ANON_KEY environment
// variables on every deploy. For local testing without running the build,
// fill these two values in by hand temporarily (do not commit real values).
window.__ENV__ = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: ""
};
